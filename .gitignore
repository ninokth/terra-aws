.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan
tfplan
terraform.tfvars
*.tfvars
crash.log
crash.*.log

# Exclude Notes directory from git
Notes/

# User-specific configuration (use user.conf.example as template)
config/user.conf

# Generated infrastructure configs (regenerated after each deploy)
config/infrastructure.json
config/infrastructure.ini
config/infrastructure.env
config/ssh_config

# Runtime state files (contents ignored, directory kept via .gitkeep)
scripts/current_state/*
!scripts/current_state/.gitkeep

# Log files
logs/

# Backup files (contents ignored, directory kept via .gitkeep)
backup/*
!backup/.gitkeep
