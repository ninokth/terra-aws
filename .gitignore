.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan
tfplan
terraform.tfvars
*.tfvars
crash.log
crash.*.log

# Exclude Notes and docs directories from git
Notes/
docs/
providers/

# Deployment scripts (internal tooling - not for public distribution)
scripts/

# Critical rules documentation (project-specific, not for repo)
.critical_rules/

# User-specific configuration (use user.conf.example as template)
config/user.conf

# Generated infrastructure configs (regenerated after each deploy)
config/infrastructure.json
config/infrastructure.ini
config/infrastructure.env
config/ssh_config

# Log files
logs/

# Backup files (contents ignored, directory kept via .gitkeep)
backup/*
!backup/.gitkeep

# Pre-commit cache
.pre-commit-cache/
