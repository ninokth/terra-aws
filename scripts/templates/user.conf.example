# User Configuration File for Terraform Bastion Infrastructure
# Copy this file to config/user.conf and customize for your environment
#
# Usage:
#   cp config/user.conf.example config/user.conf
#   vim config/user.conf  # Edit with your values
#   ./scripts/first_time_setup.sh
#
# All scripts will read from this configuration file.

# ==============================================================================
# AWS Configuration
# ==============================================================================

# AWS CLI profile name (created with: aws configure --profile <name>)
# Leave empty to use default profile or environment variables
AWS_PROFILE="terraform"

# AWS Region for deployment
# Common regions: us-east-1, us-west-2, eu-west-1, eu-north-1, ap-southeast-1
AWS_REGION="eu-north-1"

# Availability Zone (must be in the selected region)
# Examples: us-east-1a, eu-north-1a, ap-southeast-1a
AVAILABILITY_ZONE="eu-north-1a"

# ==============================================================================
# Network Configuration
# ==============================================================================

# Your public IP address for SSH access (CIDR notation)
# - Set to "auto" to detect automatically
# - Set to specific IP: "203.0.113.50/32"
# - Set to multiple IPs: "203.0.113.50/32,198.51.100.25/32" (not recommended)
ADMIN_IP_CIDR="auto"

# VPC CIDR block
VPC_CIDR="10.22.0.0/16"

# Public subnet CIDR (bastion host)
PUBLIC_SUBNET_CIDR="10.22.5.0/24"

# Private subnet CIDR (private hosts)
PRIVATE_SUBNET_CIDR="10.22.6.0/24"

# ==============================================================================
# Resource Naming
# ==============================================================================

# Prefix for all AWS resource names
# Use lowercase, alphanumeric, and hyphens only
NAME_PREFIX="tf-demo"

# ==============================================================================
# SSH Configuration
# ==============================================================================

# Path to SSH private key (public key will be .pub)
# Set to "auto" to use default ~/.ssh/id_ed25519
SSH_KEY_PATH="auto"

# SSH key type (ed25519 recommended, rsa for legacy systems)
SSH_KEY_TYPE="ed25519"

# SSH username for EC2 instances (ubuntu for Ubuntu AMIs)
SSH_USER="ubuntu"

# ==============================================================================
# Instance Configuration
# ==============================================================================

# Bastion host instance type
# t3.micro is free-tier eligible
BASTION_INSTANCE_TYPE="t3.micro"

# Private host instance type
PRIVATE_INSTANCE_TYPE="t3.micro"

# ==============================================================================
# Advanced Options (usually no changes needed)
# ==============================================================================

# Terraform working directory
TERRAFORM_DIR="providers"

# Enable verbose output in scripts (true/false)
VERBOSE="false"

# Skip confirmation prompts (true/false) - use with caution
AUTO_APPROVE="false"
